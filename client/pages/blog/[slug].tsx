import React from 'react'

type Props = {}

// article folder is for local mdx contents
// TODO: if needed: for remote mdx contents
const Article = (props: Props) => {
  return (
    <div>[slug]</div>
  )
}

export default Article